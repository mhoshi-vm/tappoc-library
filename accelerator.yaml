engine:
  merge:
    - include: [ "**" ]
      exclude: [ "src/main/resources/**", "mvnw*", ".mvn/**" ]
      chain:
        - type: RewritePath
          regex: (.*)
          rewriteTo: "'library/' + #g0"

